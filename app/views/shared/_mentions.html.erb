<div class="mentions-container">
  <strong><%= t('views.mention_relationship', name: Report.model_name.human) %></strong>
  <ul>
    <% report.mentioned_reports.each do |report| %>
      <li>
        <%= link_to report.title, report %>
      </li>
    <% end %>
  </ul>
</div>
